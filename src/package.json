{
  "name": "render-test",
  "version": "1.0.0",
  "description": "Shared TypeScript code between RenderTestService scripts, Lambdas, and other compute",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w"
  },
  "author": "sjwhalen",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.470.0",
    "@aws-sdk/client-lambda": "^3.470.0"
  }
}
